---
jupyter: python3  
execute:
  daemon: false
---

```{python}
import pandas as pd
import plotly.express as px
from inspect_viz.data import reactive_df
from inspect_viz.widgets import figure_view, table_view, menu_input

df = reactive_df(px.data.iris())
df = df.query("SELECT * WHERE species = 'setosa'")
```

```{python}
menu_input(df, column="species")
```

```{python}
fig = px.scatter(df, x="sepal_width", y="sepal_length")
figure_view(df, fig)
```

```{python}
table_view(df)
```

